{{!-- Home display when  --}}

  <div class="row align-center mb-5">
     <div class="col-md-6">
       <h2>Welcome back {{name}}!</h2>
     </div>

  {{!-- Display previous blog posts from you --}}
   <section class="flex-row justify-center">

    {{#each blogs}}
    <p> {{ title }} </p>
    <p> {{ content }} </p>
    {{/each}}

  </section>

